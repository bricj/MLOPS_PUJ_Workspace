apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - inference-models.yaml
  - gradio.yaml
  - prometheus.yaml

commonLabels:
  app.kubernetes.io/managed-by: argocd
  app.kubernetes.io/part-of: servicios-api